import React from 'react';
import './index.css';

export default function DesktopIcon(image) {
    return (
        <div className="desktop-icon">
            <img src={image} />
        </div>
    )
}